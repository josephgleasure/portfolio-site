# Project Context

## What to keep
- Responsive mobiledesktop layout
- Two-column mobile grid, symmetric padding, no horizontal scroll
- Modal viewer single image on mobile, arrow nav
- SPA with Netlify `_redirects`

## What to change
- Replace isometric grid with CSS Grid
- Remove sidebardropdown
- New color theme (tokens)
- Lighter asset count; GitHub Pages or Netlify deploy

## Grid spec
- Mobile 2 cols, gap 10–12px, padding 12px sides
- Tablet 3–4 cols; Desktop 13 cols, 5 rows
- dotted placeholders for empty cells
- skip some empty cells entirely
- Entrance animation fadescale stagger

## Modal spec
- Mobile 1 image per view
- Desktop 1–2 if wide; keyboard ←→

## Theming
- root tokens --bg, --text, --muted, --card, --accent, --gap, --radius

## Deploy
- Netlify Drop (dist + _redirects) or GH Pages (set vite base)

## Next steps
1) Scaffold Project type + projects.json
2) Build ProjectGrid (CSS Grid) + placeholders
3) Build ProjectModal
4) Apply theme tokens
5) Wire routes and test